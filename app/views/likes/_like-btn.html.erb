<% if diary.liked_by?(current_user) %>
  <%= link_to diary.likes.count, diary_likes_path(diary), method: :delete, remote: true %>
<% else %>
  <%= link_to diary.likes.count, diary_likes_path(diary), method: :post, remote: true %>
<% end %>